extends base
block content
  nav(aria-label="breadcrumb")
    ol.breadcrumb
      li.breadcrumb-item
        a(href="/") Home
      li.breadcrumb-item.active(aria-current="page") My Account 
  .container-fluid
    .row
      .col-12.m-5
        h1.h2= `Dashboard ${user.name}` 
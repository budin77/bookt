extends base
block content
  nav(aria-label="breadcrumb")
    ol.breadcrumb
      li.breadcrumb-item
        a(href="/") Home
      li.breadcrumb-item.active(aria-current="page") My Account 
  .alert.alert-success.m-5
    h4.alert-heading= `Dashboard ${user.name}`
    hr
